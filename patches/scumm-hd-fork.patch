diff --git a/engines/scumm/gfx.cpp b/engines/scumm/gfx.cpp
index fb78f408..418a402c 100644
--- a/engines/scumm/gfx.cpp
+++ b/engines/scumm/gfx.cpp
@@ -30,6 +30,7 @@
 #include "scumm/scumm_v5.h"
 #include "scumm/scumm_v6.h"
 #include "scumm/usage_bits.h"
+#include "scumm/hd_asset_manager.h"
 #include "scumm/he/wiz_he.h"
 #include "scumm/util.h"
 
@@ -542,6 +543,17 @@ void ScummEngine::drawDirtyScreenParts() {
 	} else {
 		updateDirtyScreen(kMainVirtScreen);
 	}
+
+	// HD: blit the 4K background image directly at native resolution
+	if (_hdBackgroundSurface.getPixels()) {
+		_system->copyRectToScreen(
+			_hdBackgroundSurface.getPixels(),
+			_hdBackgroundSurface.pitch,
+			0, 0,
+			_hdBackgroundSurface.w,
+			_hdBackgroundSurface.h
+		);
+	}
 }
 
 void ScummEngine_v6::drawDirtyScreenParts() {
@@ -1179,6 +1191,27 @@ void ScummEngine::redrawBGStrip(int start, int num) {
 		room = getResourceAddress(rtRoom, _roomResource);
 
 	_gdi->drawBitmap(room + _IM00_offs, &_virtscr[kMainVirtScreen], s, 0, _roomWidth, _virtscr[kMainVirtScreen].h, s, num, 0);
+
+	// HD background: overwrite the rendered strips with HD data
+	if (_hdCurrentRoom >= 0 && _hdBackgroundSurface.getPixels()) {
+		VirtScreen *vs = &_virtscr[kMainVirtScreen];
+		float scaleX = (float)_hdBackgroundSurface.w / _roomWidth;
+		float scaleY = (float)_hdBackgroundSurface.h / vs->h;
+
+		for (int i = 0; i < num; i++) {
+			int stripIdx = s + i;
+			int stripX = stripIdx * 8;
+			for (int y = 0; y < vs->h; y++) {
+				uint32 hdY = (uint32)(y * scaleY);
+				for (int x = 0; x < 8 && (stripX + x) < vs->w; x++) {
+					uint32 hdX = (uint32)((stripX + x) * scaleX);
+					const uint8 *p = (const uint8 *)_hdBackgroundSurface.getBasePtr(hdX, hdY);
+					uint32 idx = findClosestPaletteColor(_currentPalette, 256, p[0], p[1], p[2]);
+					*(byte *)vs->getBasePtr(stripX + x, y) = (byte)idx;
+				}
+			}
+		}
+	}
 }
 
 void ScummEngine::restoreBackground(Common::Rect rect, byte backColor) {
diff --git a/engines/scumm/module.mk b/engines/scumm/module.mk
index c3fadf55..0676991e 100644
--- a/engines/scumm/module.mk
+++ b/engines/scumm/module.mk
@@ -22,6 +22,7 @@ MODULE_OBJS := \
 	gfx_nes.o \
 	gfx_towns.o \
 	gfx.o \
+	hd_asset_manager.o \
 	he/mixer_he.o \
 	he/resource_he.o \
 	he/script_v60he.o \
diff --git a/engines/scumm/room.cpp b/engines/scumm/room.cpp
index e6a6109e..dc23edd9 100644
--- a/engines/scumm/room.cpp
+++ b/engines/scumm/room.cpp
@@ -26,6 +26,7 @@
 #ifdef ENABLE_HE
 #include "scumm/he/intern_he.h"
 #endif
+#include "scumm/hd_asset_manager.h"
 #include "scumm/object.h"
 #include "scumm/resource.h"
 #include "scumm/scumm_v3.h"
@@ -298,6 +299,17 @@ void ScummEngine::startScene(int room, Actor *a, int objectNr) {
 			_system->setFeatureState(OSystem::kFeatureVirtualKeyboard, true);
 	}
 
+	// Try to load the HD background for this room
+	_hdBackgroundSurface.free();
+	_hdCurrentRoom = -1;
+	if (_hdScale > 1 && _hdAssetManager->hasBackground(room)) {
+		if (_hdAssetManager->loadBackground(room, _hdBackgroundSurface)) {
+			_hdCurrentRoom = room;
+			debug(2, "Loaded HD background for room %d (%dx%d)", room,
+				_hdBackgroundSurface.w, _hdBackgroundSurface.h);
+		}
+	}
+
 }
 
 /**
diff --git a/engines/scumm/scumm.cpp b/engines/scumm/scumm.cpp
index 1b65e51c..4a18b493 100644
--- a/engines/scumm/scumm.cpp
+++ b/engines/scumm/scumm.cpp
@@ -47,6 +47,7 @@
 #include "scumm/dialogs.h"
 #include "scumm/file.h"
 #include "scumm/file_nes.h"
+#include "scumm/hd_asset_manager.h"
 #include "scumm/imuse/imuse.h"
 #include "scumm/imuse_digi/dimuse_engine.h"
 #include "scumm/smush/smush_player.h"
@@ -155,6 +156,7 @@ ScummEngine::ScummEngine(OSystem *syst, const DetectorResult &dr)
 		_gdi = new Gdi(this);
 	}
 	_res = new ResourceManager(this);
+	_hdAssetManager = new HDAssetManager(this);
 
 	// Convert MD5 checksum back into a digest
 	for (int i = 0; i < 16; ++i) {
@@ -537,6 +539,7 @@ ScummEngine::~ScummEngine() {
 #endif
 
 	delete _res;
+	delete _hdAssetManager;
 	delete _gdi;
 }
 
@@ -981,6 +984,27 @@ Common::Error ScummEngine::init() {
 
 	const Common::FSNode gameDataDir(ConfMan.getPath("path"));
 
+	// Initialize HD asset path
+	Common::String hdPath = gameDataDir.getPath().toString();
+	// Ensure no double slash
+	if (hdPath.lastChar() != '/' && hdPath.lastChar() != '\\')
+		hdPath += '/';
+	hdPath += "hd";
+	_hdAssetManager->setHDPath(hdPath);
+	// Check if the HD directory was found (setHDPath will clear _hdPath if not)
+	if (_hdAssetManager->isEnabled()) {
+		_hdScale = _hdAssetManager->getScale();
+		warning("HD mode enabled, scale=%d, path=%s", _hdScale, hdPath.c_str());
+
+		// Preload room 87 (difficulty select) for testing overlay
+		_hdBackgroundSurface.free();
+		if (_hdAssetManager->loadBackground(87, _hdBackgroundSurface)) {
+			_hdCurrentRoom = 87;
+			warning("HD: preloaded room 87 bg (%dx%d)",
+				_hdBackgroundSurface.w, _hdBackgroundSurface.h);
+		}
+	}
+
 	for (uint i = 0; ruScummPatcherTable[i].patcherName; i++) {
 		if (ruScummPatcherTable[i].gameid == _game.id && (_game.variant == nullptr || strcmp(_game.variant, ruScummPatcherTable[i].variant) == 0)) {
 			Common::File *f = new Common::File();
@@ -1502,6 +1526,15 @@ Common::Error ScummEngine::init() {
 
 	_outputPixelFormat = _system->getScreenFormat();
 
+	// If HD mode is active, resize display to 4K for true HD rendering
+	if (_hdScale > 1) {
+		Graphics::PixelFormat rgbaFmt(4, 8, 8, 8, 8, 24, 16, 8, 0);
+		initGraphics(2560, 1920, &rgbaFmt);
+		_outputPixelFormat = _system->getScreenFormat();
+		warning("HD: display set to %dx%dx%dbpp (true 4K)",
+			2560, 1920, _outputPixelFormat.bytesPerPixel * 8);
+	}
+
 	setupScumm(macResourceFile);
 
 	readIndexFile();
diff --git a/engines/scumm/scumm.h b/engines/scumm/scumm.h
index 928cebd9..95095096 100644
--- a/engines/scumm/scumm.h
+++ b/engines/scumm/scumm.h
@@ -100,6 +100,7 @@ class ScummDebugger;
 class Sound;
 class Localizer;
 class GlyphRenderer_v7;
+class HDAssetManager;
 
 struct Box;
 struct BoxCoords;
@@ -545,6 +546,12 @@ public:
 	/** Graphics manager */
 	Gdi *_gdi = nullptr;
 
+	/** HD asset manager (for upscaled external textures) */
+	HDAssetManager *_hdAssetManager = nullptr;
+	int _hdScale = 1;
+	Graphics::Surface _hdBackgroundSurface;
+	int _hdCurrentRoom = -1;
+
 	/** Central resource data. */
 	ResourceManager *_res = nullptr;
 	int _insideCreateResource = 0; // Counter for HE sound
